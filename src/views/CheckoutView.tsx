import { useEffect, useState } from "react"

export default function CheckoutView() {
	const test = {
		name: "test",
		email: "test@test.com",
		address: "test",
	}
	console.log(test.name);

	return (
		<div className="checkout">
			<h1 className="text-center font-bold">WIP</h1>
		</div>
	)
}
